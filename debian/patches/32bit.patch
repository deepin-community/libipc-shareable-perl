Description: Mark tests as TODO which fail on 32bit architectures.
Origin: vendor
Bug: https://github.com/stevieb9/ipc-shareable/issues/14
Bug-Debian: https://bugs.debian.org/997829
Author: gregor herrmann <gregoa@debian.org>
Last-Update: 2022-03-21

--- a/t/65-seg_size.t
+++ b/t/65-seg_size.t
@@ -9,7 +9,7 @@
     if (! $ENV{CI_TESTING}) {
         plan skip_all => "Not on a legit CI platform...";
     }
-    if ($Config{ivsize} < 8) {
+    if ($Config{ivsize} < 8 || $Config{sizesize} < 8) {
         plan skip_all => "This test script can't be run on a perl < 64-bit";
     }
 }
